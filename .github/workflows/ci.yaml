name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Lint Kubernetes manifests
        run: echo "kubectl dry-run validation will go here"

      - name: Validate Sloth specs
        run: echo "sloth validation will go here"